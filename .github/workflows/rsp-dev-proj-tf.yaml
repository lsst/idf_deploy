# Deploys Science Platform Dev GCP Project with Terraform
name: 'RSP Dev GCP Project'

on:
  pull_request:
    paths:
      - 'environment/deployments/science-platform/env/dev.tfvars'
  push:
    paths:
      - 'environment/deployments/science-platform/env/dev.tfvars'
    branches:
      - main
  # Re-enable this when/if we want to deal with drift
  # schedule:
  # - cron: 25 2 * * *

jobs:
  terraform:
    name: Terraform
    uses: ./.github/workflows/_reusable-terraform-plan-apply.yaml
    with:
      working_directory: ./environment/deployments/science-platform
      state_prefix: science-platform/dev
      tfvars_file: env/dev.tfvars
      gcp_credentials_secret: PIPELINE_RSP_DEV_PROJECT
    secrets: inherit
