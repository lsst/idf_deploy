# Deploys Science Platform Dev GCP Project with Terraform
name: 'RSP DEV ALERTDB'

on:
  pull_request:
    paths:
      - 'environment/deployments/science-platform/env/dev-alertdb.tfvars'
  push:
    branches:
      - main
    paths:
      - 'environment/deployments/science-platform/env/dev-alertdb.tfvars'
  # Re-enable this when/if we want to deal with drift
  # schedule:
  # - cron: 25 2 * * *

jobs:
  terraform:
    name: Terraform
    uses: ./.github/workflows/_reusable-terraform-plan-apply.yaml
    with:
      working_directory: ./environment/deployments/science-platform/alertdb
      state_prefix: science-platform/dev/alertdb
      tfvars_file: ../env/dev-alertdb.tfvars
    secrets: inherit
