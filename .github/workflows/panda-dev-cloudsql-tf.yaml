# Deploys PanDA Dev GCP Project with Terraform
name: 'PANDA DEV CLOUD SQL'

on:
  pull_request:
    paths:
      - 'environment/deployments/panda/env/dev-cloudsql.tfvars'
  push:
    branches:
      - main
    paths:
      - 'environment/deployments/panda/env/dev-cloudsql.tfvars'
  # Re-enable this when/if we want to deal with drift
  # schedule:
  # - cron: 25 2 * * *

jobs:
  terraform:
    name: Terraform
    uses: ./.github/workflows/_reusable-terraform-plan-apply.yaml
    with:
      working_directory: ./environment/deployments/panda/cloudsql
      state_prefix: panda/dev/cloudsql
      tfvars_file: ../env/dev-cloudsql.tfvars
    secrets: inherit
