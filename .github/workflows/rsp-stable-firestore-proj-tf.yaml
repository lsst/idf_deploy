# Deploys Science Platform Firestore Stable GCP Project with Terraform
name: 'RSP STABLE FIRESTORE GCP Project'

on:
  pull_request:
    paths:
      - 'environment/deployments/science-platform/firestore/env/production.tfvars'
  push:
    paths:
      - 'environment/deployments/science-platform/firestore/env/production.tfvars'
    branches:
      - main
  # Re-enable this when/if we want to deal with drift
  # schedule:
  # - cron: 25 2 * * *

jobs:
  terraform:
    name: Terraform
    uses: ./.github/workflows/_reusable-terraform-plan-apply.yaml
    with:
      working_directory: ./environment/deployments/science-platform/firestore
      state_prefix: science-platform/firestore/prod
      tfvars_file: env/production.tfvars
    secrets: inherit
