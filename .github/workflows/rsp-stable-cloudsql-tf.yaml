# Deploys Science Platform Stable GCP Project with Terraform
name: 'RSP STABLE CLOUD SQL'

on:
  pull_request:
    paths:
      - 'environment/deployments/science-platform/env/production-cloudsql.tfvars'
  push:
    branches:
      - main
    paths:
      - 'environment/deployments/science-platform/env/production-cloudsql.tfvars'
  # Re-enable this when/if we want to deal with drift
  # schedule:
  # - cron: 25 2 * * *

jobs:
  terraform:
    name: Terraform
    uses: ./.github/workflows/_reusable-terraform-plan-apply.yaml
    with:
      working_directory: ./environment/deployments/science-platform/cloudsql
      state_prefix: science-platform/stable/cloudsql
      tfvars_file: ../env/production-cloudsql.tfvars
    secrets: inherit
