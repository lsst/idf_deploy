# Deploys Butler Integration VPC Peering with Terraform
name: 'Butler DEV VPC Peering'

on:
  pull_request:
    paths:
      - 'environment/deployments/butler-repo/env/dev-vpcpeer.tfvars'
  push:
    branches:
      - main
    paths:
      - 'environment/deployments/butler-repo/env/dev-vpcpeer.tfvars'
  # Re-enable this when/if we want to deal with drift
  # schedule:
  # - cron: 25 2 * * *

jobs:
  terraform:
    name: Terraform
    uses: ./.github/workflows/_reusable-terraform-plan-apply.yaml
    with:
      working_directory: ./environment/deployments/butler-repo/vpc_peering
      state_prefix: butler-repo/dev/vpc_peer
      tfvars_file: ../env/dev-vpcpeer.tfvars
    secrets: inherit
